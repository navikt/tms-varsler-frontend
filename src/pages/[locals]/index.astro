---
import Layout from "@src/layout/Layout.astro";
import PageHeader from "@components/PageHeader/PageHeader.astro";
import Varsler from "@components/Varsler/Varsler.astro";
import {Loader} from "@navikt/ds-react";
import {Locale} from "@src/customTypes/Locale";
import {text} from "@src/language/text";
const currentLocale = Astro.currentLocale as Locale
---

<Layout>
    <PageHeader />
    <Varsler server:defer>
        <Loader size="3xlarge" slot="fallback" title={text.loadingMessage[currentLocale]}/>
    </Varsler>
</Layout>

